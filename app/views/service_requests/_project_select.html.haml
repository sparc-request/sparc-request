.inline
  = t("study_assignment.select_protocol")
  = collection_select :service_request, :protocol_id, projects, :id, :display_protocol_id_and_title, {:include_blank => t("study_assignment.select_project")}, :class => 'edit_project_id'
  .edit-project.blue-button= t("study_assignment.edit_project")

- if @sub_service_request.nil?
  %br
  %span= t("study_assignment.or")
  %br
  %br
