%table
  %thead
    %tr
      %th= t("cm_shared.labels.source")
      %th= t("cm_shared.labels.rate")
  %tbody
    %tr
      %th= t("cm_shared.labels.college")
      %td= select_tag "pricing_setups[#{rates.try(:[], 'id') || 'blank_pricing_setup'}][college_rate_type]", options_for_select(Service::RATE_TYPES.map{|p| [p[:display], p[:value]]}, rates.try(:[], 'college_rate_type')), :prompt => '--- Select ---', :disabled => pricing_setup_disabled, :readonly => pricing_setup_disabled, :class => "rate #{rates ? 'validate' : ''}"
    %tr
      %th= t("cm_shared.labels.federal")
      %td= select_tag "pricing_setups[#{rates.try(:[], 'id') || 'blank_pricing_setup'}][federal_rate_type]", options_for_select(Service::RATE_TYPES.map{|p| [p[:display], p[:value]]}, rates.try(:[], 'federal_rate_type')), :prompt => '--- Select ---', :disabled => pricing_setup_disabled, :readonly => pricing_setup_disabled, :class => "rate #{rates ? 'validate' : ''}"
    %tr
      %th= t("cm_shared.labels.foundation")
      %td= select_tag "pricing_setups[#{rates.try(:[], 'id') || 'blank_pricing_setup'}][foundation_rate_type]", options_for_select(Service::RATE_TYPES.map{|p| [p[:display], p[:value]]}, rates.try(:[], 'foundation_rate_type')), :prompt => '--- Select ---', :disabled => pricing_setup_disabled, :readonly => pricing_setup_disabled, :class => "rate #{rates ? 'validate' : ''}"
    %tr
      %th= t("cm_shared.labels.industry")
      %td= select_tag "pricing_setups[#{rates.try(:[], 'id') || 'blank_pricing_setup'}][industry_rate_type]", options_for_select(Service::RATE_TYPES.map{|p| [p[:display], p[:value]]}, rates.try(:[], 'industry_rate_type')), :prompt => '--- Select ---', :disabled => pricing_setup_disabled, :readonly => pricing_setup_disabled, :class => "rate #{rates ? 'validate' : ''}"
    %tr
      %th= t("cm_shared.labels.investigator")
      %td= select_tag "pricing_setups[#{rates.try(:[], 'id') || 'blank_pricing_setup'}][investigator_rate_type]", options_for_select(Service::RATE_TYPES.map{|p| [p[:display], p[:value]]}, rates.try(:[], 'investigator_rate_type')), :prompt => '--- Select ---', :disabled => pricing_setup_disabled, :readonly => pricing_setup_disabled, :class => "rate #{rates ? 'validate' : ''}"
    %tr
      %th= t("cm_shared.labels.internal")
      %td= select_tag "pricing_setups[#{rates.try(:[], 'id') || 'blank_pricing_setup'}][internal_rate_type]", options_for_select(Service::RATE_TYPES.map{|p| [p[:display], p[:value]]}, rates.try(:[], 'internal_rate_type')), :prompt => '--- Select ---', :disabled => pricing_setup_disabled, :readonly => pricing_setup_disabled, :class => "rate #{rates ? 'validate' : ''}"
    %tr
      %th &nbsp;
      %td.footnote= t("cm_shared.labels.required_field")