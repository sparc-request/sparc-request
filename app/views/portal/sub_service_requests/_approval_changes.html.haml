%h3 Approval History
%table{:id => 'approval_history_table', :class => 'tablesorter'}
  %thead
    %tr
      %th Date Approved
      %th Type
      %th Approved By
  %tbody
    - @approvals.each do |approval|
      %tr
        %td= approval.try(:approval_date).try(:strftime, "%D")
        %td= approval.try(:approval_type)
        %td= approval.try(:identity).try(:full_name).try(:humanize)
