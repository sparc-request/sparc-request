= javascript_include_tag 'protocol'

= form_tag navigate_service_request_path(@service_request), :id => 'navigation_form' do
  = hidden_field_tag :location, ''
  = hidden_field_tag :validates, ''
  = render :partial => 'steps'
  = render :partial => 'errors'
  .grid_9
    .select-project-view
      .dotted-border.left
        .instruction_area
          %p.instructions Please review the SPARC Services you are requesting below.
          %p.instructions
            %span.red Please note that any "Professional Services" listed below have UMA fees associated with them and cannot be removed from this screen.
            If Professional Services are covered by % effort, you will have the opportunity to indicate this in Step 2.
          %p.instructions If you wish to add services, you must return to the "Service Catalog".

        .service-list= render :partial => 'service_list'
  
        %br
        %br

        %p.instructions.center Before continuing to Step 2, you must select a Research Study or Project to which the services selected are associated. Only 1 Research Study/Project can be assigned to this request.

        .type-select.center
          - if @sub_service_request.nil?
            Is this Service Request tied to a particular Research Study
            = select_tag "select-type", options_for_select([["Yes"], ["No"]], (@service_request.protocol.try(:type) == 'Project' ? 'No' : 'Yes'))
            %span#infotip= image_tag('information.png')
            %br
            %br

        - if (@service_request.protocol.try(:type) == 'Study')
          .existing-study.center
            #study.study-select
              #study-select= render :partial => 'study_select', :locals => {:studies => @studies} unless @studies.empty?
              = link_to('New Study', new_service_request_study_path(@service_request), :class => 'new-study') if @sub_service_request.nil?
              %br
              %br

        - if (@service_request.protocol.try(:type) == 'Project')
          .existing-project.center
            #project.project-select
              #project-select= render :partial => 'project_select', :locals => {:projects => @projects} unless @projects.empty?
              = link_to('New Project', new_service_request_project_path(@service_request), :class => 'new-project') if @sub_service_request.nil?
              %br
              %br

  .grid_3.protocol-view-right.right
    #services.ui-widget.ui-widget-content.cart-view
      %h3
        My Services
        %span#cart-help Help
      .line-items= render :partial => 'catalogs/cart'
    .dashboard-button= navigation_link image_tag('back_to_catalog.png'), @back
.continue= render :partial => 'service_requests/navigation'
