.user-edit-protocol-view
  :javascript
    $(document).ready(function() {
    Sparc.project.ready();
    });
  
  .grid_7.box
    .info
      .field
        = f.label :short_title, "Short Title:*"
        = f.text_field :short_title

      .field
        = f.label :title, "Project Title:*"
        = f.text_field :title
        
      .field
        = f.label :funding_status, "Proposal Funding Status:*"
        = f.select :funding_status, options_for_select(FUNDING_STATUSES, project.funding_status), :include_blank => "Select a Funding Status"
      
      #funded
        .field
          = f.label :funding_source, "Funding Source:*"
          = f.select :funding_source, options_for_select(FUNDING_SOURCES, project.funding_source), :include_blank => "Select a Funding Source"

      #pending_funding 
        .field
          = f.label :potential_funding_source, "Potential Funding Source:*"
          = f.select :potential_funding_source, options_for_select(POTENTIAL_FUNDING_SOURCES, project.potential_funding_source), :include_blank => "Select a Potential Funding Source"

      .field
        = f.label :brief_description, "Brief Description:"
        = f.text_area :brief_description

      .indirect_cost_rate
        = f.label :indirect_cost_rate, "Indirect Cost Rate:"
        = f.text_field :indirect_cost_rate
  
  %br.clear
  #actions
    = submit_tag("Save project")
    or
    = link_to "Cancel", root_path
    
