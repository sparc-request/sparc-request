.inline
  = t("study_assignment.select_project_long")
  = collection_select :service_request, :protocol_id, projects, :id, :short_title, {:include_blank => t("study_assignment.select_project")}, :class => 'edit_project_id'
  %button.edit-project= t("study_assignment.edit_project")

- if @sub_service_request.nil?
  %br
  %span= t("study_assignment.or")
  %br
  %br
